<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Exact alarm scheduling (survives Doze); Android 12+ requires this be
         requested from the user, which AlarmSchedulingService does at
         alarm-creation time. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Lets the alarm notification launch AlarmRingScreen directly over the
         lock screen instead of just showing a heads-up notification. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- Required on Android 13+ to post the scheduling notification at all. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Used by the `vibration` package for the ring screen's buzz pattern. -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Keeps the CPU awake long enough for the exact alarm to actually fire
         and the full-screen intent to launch. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Lets flutter_local_notifications reschedule alarms after a reboot,
         since AlarmManager entries are otherwise cleared. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Camera + on-device pose detection for framing/exercise verification.
         Requested at runtime by CameraPermissionService, not just declared. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- AlarmRingService: the foreground service that owns alarm sound +
         vibration independently of any Activity, so backgrounding/swiping
         the app away can never silence it. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <application
        android:label="motion_alarm"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:resizeableActivity="false"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Required by flutter_local_notifications to actually deliver a
             scheduled alarm: without this, AlarmManager fires but Android has
             nowhere to broadcast to, and silently drops it (this was the bug
             that made every alarm schedule successfully but never fire). -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <!-- Reschedules alarms after a reboot (AlarmManager entries don't
             survive one otherwise). -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Owns alarm ringing independently of any Activity. Started by
             AlarmRingReceiver, which is fired by a native AlarmManager entry
             scheduled in parallel with flutter_local_notifications' own
             (see MainActivity.scheduleRing / NativeAlarmRingBridge). -->
        <service
            android:name=".AlarmRingService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
        <receiver
            android:name=".AlarmRingReceiver"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
